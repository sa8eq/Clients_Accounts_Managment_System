# Clients_Accounts_Managment_System

# 🏦 نظام إدارة حسابات العملاء البنكية (Client Account Management System)

هذا البرنامج هو نظام بسيط لإدارة حسابات العملاء البنكية، مكتوب بلغة **C++**. يتيح البرنامج للمستخدمين (مديري النظام) تنفيذ عمليات أساسية مثل إضافة العملاء، عرض القائمة، تحديث بياناتهم، حذفهم، البحث عنهم، وإجراء معاملات الإيداع والسحب، بالإضافة إلى عرض إجمالي الأرصدة.

## ✨ المميزات الرئيسية

* **إدارة العملاء:** إضافة، حذف، وتحديث معلومات العملاء.
* **عرض القائمة:** عرض قائمة مفصلة بجميع العملاء وأرصدتهم.
* **البحث عن العملاء:** البحث عن عميل معين باستخدام رقم الحساب.
* **المعاملات المالية:** إيداع الأموال (Deposit) وسحبها (Withdrawal) من الحسابات.
* **عرض الأرصدة الإجمالية:** حساب وعرض مجموع أرصدة جميع العملاء.
* **تخزين البيانات:** حفظ بيانات العملاء في ملف نصي خارجي (`Clients.txt`) لضمان استمراريتها بين جلسات التشغيل.

## 🛠️ متطلبات التشغيل

لترجمة وتشغيل هذا البرنامج، تحتاج إلى:

1.  **مترجم (Compiler) للغة C++** يدعم معيار C++ الحديث (مثل GCC, Clang, أو MSVC).
2.  **نظام تشغيل** يدعم مكتبة `iostream` و `fstream` القياسية. (البرنامج يستخدم وظيفة `system("cls")` و `system("pause>0")` الخاصة بنظام التشغيل Windows لإدارة الشاشة، والتي قد تحتاج إلى تعديل أو استبدال في أنظمة Unix/Linux).

## 🚀 كيفية التشغيل

1.  **حفظ الكود:** احفظ الكود المصدري بالكامل في ملف واحد (مثل `bank_system.cpp`).
2.  **الترجمة:** قم بترجمة الملف باستخدام مترجم C++:
    ```bash
    g++ bank_system.cpp -o bank_system
    ```
    (إذا كنت تستخدم GCC).
3.  **التنفيذ:** قم بتشغيل الملف التنفيذي الناتج:
    ```bash
    ./bank_system
    ```
    (أو `bank_system.exe` على نظام Windows).
4.  **بدء العمل:** سيتم عرض **القائمة الرئيسية (Main Menu)**، ويمكنك اختيار العمليات المطلوبة.

## 📁 هيكلية البيانات والتخزين

* **ملف البيانات:** يتم تخزين جميع بيانات العملاء في ملف نصي يسمى `Clients.txt`.
* **صيغة التخزين:** يتم تخزين كل سجل عميل على سطر منفصل، ويتم فصل حقول البيانات (رقم الحساب، الرمز السري، الاسم، الهاتف، الرصيد) باستخدام محدد خاص هو `\#/\#/\#`.

| الحقل            | النوع في C++ | الوصف                 |
| :--------------- | :----------- | :-------------------- |
| AccountNumber    | `string`     | رقم الحساب (المعرف)   |
| PinCode          | `string`     | الرمز السري           |
| Name             | `string`     | اسم العميل            |
| Phone            | `string`     | رقم الهاتف            |
| Balance          | `double`     | الرصيد الحالي للحساب |

## ⚙️ تفاصيل التنفيذ والوظائف

### **التركيب الأساسي**

* **`stClient`:** هيكل البيانات (struct) المستخدم لتمثيل سجل العميل الواحد.
* **`enOption` / `enTransOption`:** تعدادات (enums) تستخدم لتسهيل إدارة خيارات القائمة الرئيسية وقائمة المعاملات.
* **`ChooseOption(short From, short To)`:** وظيفة لضمان إدخال المستخدم لخيار صحيح ضمن النطاق المحدد.

### **وظائف الملفات والبيانات**

* **`LoadDataFromFile(string FileName)`:** تقرأ جميع بيانات العملاء من الملف النصي وتُعيدها كـ `vector<stClient>`.
* **`ConvertLineToRecord(...)`:** تحول السطر النصي المقروء من الملف إلى هيكل `stClient`.
* **`ConverRecordToLine(stClient C)`:** تحول هيكل `stClient` إلى سطر نصي جاهز للكتابة في الملف.
* **`SplitWords(...)`:** وظيفة مساعدة لتقسيم السطر النصي باستخدام محدد (`#//#`).
* **`SaveChangesToFile(string FileName, vector<stClient> vClients)`:** تكتب القائمة المحدثة للعملاء مرة أخرى إلى الملف، متجاوزة أي عملاء تم وضع علامة `MarkForDelete = true` لهم.

### **وظائف الشاشة والقوائم**

* **`ShowMainMenuScreen()`:** تعرض القائمة الرئيسية.
* **`ShowTransactionScreen()`:** تعرض قائمة المعاملات (الإيداع، السحب، الأرصدة الإجمالية).
* **`ReturnToMainMenuScreen()` / `ReturnToTransactionMenu()`:** وظائف مساعدة لإيقاف مؤقت والعودة إلى القوائم الرئيسية.
* **`ShowClientsListScreen()`:** تعرض جدولاً منسقاً لجميع العملاء.
* **`PrintClientCard(stClient C)`:** تعرض تفاصيل عميل واحد بتنسيق بطاقة.

### **وظائف العمليات**

* **`FindClientByAccoutNumber(...)`:** تبحث في الملف مباشرة لتحديد ما إذا كان رقم الحساب موجوداً (مستخدمة عند إضافة عميل جديد).
* **`SearchClientByAccountNumber(...)`:** تبحث في الـ `vector` المُحمّل من الملف عن عميل محدد.
* **`UpdateAccountBalance(...)`:** وظيفة عامة لتحديث رصيد العميل (تُستخدم لكل من الإيداع والسحب).
* **`ShowDepositScreen()` / `ShowWithdrawlScreen()`:** وظائف لمعالجة معاملات الإيداع والسحب، بما في ذلك التحقق من صحة المبلغ (على سبيل المثال، عدم سحب أكثر من الرصيد المتاح).

---

## 🛑 ملاحظات مهمة

* **الأمان:** الرمز السري (PinCode) مخزن كـ `string` ولا يتم تشفيره في ملف البيانات. هذا البرنامج مُعد لأغراض التدريب والتوضيح فقط **وليس للاستخدام في بيئة إنتاجية حقيقية تتطلب مستوى عالٍ من الأمان**.
* **إدارة الأخطاء:** لا يتم التعامل مع أخطاء إدخال المستخدم بشكل كامل في جميع الحالات، خصوصاً في قراءة الأرقام (`cin >> C.Balance`).
* **منصة التشغيل:** وظائف `system("cls")` و `system("pause>0")` خاصة بـ Windows. لتشغيل البرنامج على Linux/macOS، قد تحتاج إلى استبدالها بـ `system("clear")` وإزالة وظيفة `pause`.

## 🤝 المساهمة

إذا كنت ترغب في تحسين هذا المشروع، يمكنك النظر في:

* تحسين واجهة المستخدم الرسومية (UI/UX) باستخدام مكتبات طرف ثالث.
* تطبيق خوارزميات التشفير لتأمين البيانات في ملف `Clients.txt`.
* تحسين عملية البحث والكتابة لتجنب تحميل جميع البيانات في الذاكرة لكل عملية.
